# Keystore Configuration Example
# 
# IMPORTANT: 
# 1. Copy this file to keystore.properties (without .example)
# 2. Fill in your actual keystore details
# 3. NEVER commit keystore.properties to version control
# 4. Keep your keystore file and passwords secure
#
# To generate a keystore, run:
# keytool -genkey -v -keystore facto-release.keystore -alias facto -keyalg RSA -keysize 2048 -validity 10000

storePassword=YOUR_KEYSTORE_PASSWORD_HERE
keyPassword=YOUR_KEY_PASSWORD_HERE
keyAlias=facto
storeFile=app/facto-release.keystore

